import React from 'react'

function Home() {
  return (
    <main className="max-w-3xl mx-auto p-6">
      <h2 className="text-2xl font-semibold mb-4">Página principal</h2>
      <p className="mb-2">Esta es la nueva página por defecto (ruta <code>/</code>).</p>
      <p>La barra de navegación se muestra en la parte superior (componente <code>Navbar</code>).</p>
    </main>
  )
}

export default Home
